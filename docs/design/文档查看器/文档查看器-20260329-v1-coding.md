# 编码摘要 — 文档查看器 v1

## 变更记录
- v1: 初始版本，基于文件系统扫描构建目录树 + Qdrant 语义检索 + 前端 Markdown 渲染

---

## 涉及类清单

| 全类名 | 所属模块 | 职责 |
|---|---|---|
| `com.exceptioncoder.llm.api.controller.DocViewerController` | llm-api | HTTP 入口，4 个端点 |
| `com.exceptioncoder.llm.application.service.DocViewerService` | llm-application | 目录树构建、文件读取、AI 检索编排 |
| `com.exceptioncoder.llm.domain.model.DocTreeNode` | llm-domain | 目录树节点模型 |
| `com.exceptioncoder.llm.domain.model.DocContent` | llm-domain | 文档内容响应模型 |
| `com.exceptioncoder.llm.domain.model.DocSearchResult` | llm-domain | 检索结果模型 |
| `com.exceptioncoder.llm.domain.repository.VectorStoreRepository` | llm-domain | Qdrant 向量操作抽象接口 |
| `com.exceptioncoder.llm.infrastructure.repository.QdrantVectorStoreRepository` | llm-infrastructure | Qdrant 实现（复用现有） |

---

## 接口设计

| 方法 | 路径 | 方法签名 |
|---|---|---|
| GET | `/api/v1/docs/tree` | `DocViewerController.getDocTree(): ResponseEntity<DocTreeResponse>` |
| GET | `/api/v1/docs/content` | `DocViewerController.getContent(@RequestParam path): ResponseEntity<DocContent>` |
| GET | `/api/v1/docs/search` | `DocViewerController.search(@RequestParam keyword, @RequestParam topK): ResponseEntity<DocSearchResponse>` |
| POST | `/api/v1/docs/index` | `DocViewerController.indexDocs(): ResponseEntity<Void>` |

---

## 核心业务规则

| 规则 | 说明 |
|---|---|
| 仅支持 .md 文件 | 其他文件（.sql、.drawio）不在目录树中展示 |
| 路径穿越防护 | `path` 参数必须以 `docs/` 开头，不含 `..` |
| Qdrant 不可用时降级 | 检索接口返回 503，前端提示服务不可用 |
| 索引 ID 格式 | `doc:{path}`，如 `doc:docs/design/secretary-design.md` |
| 向量模型 | 复用现有 `qwen-embedding` 配置 |

---

## 路径安全校验

读取文件前必须校验：
1. 路径以 `docs/` 开头
2. 不含 `..`（路径穿越）
3. 文件存在且为 `.md` 结尾

---

## 缓存策略

| 内容 | 策略 | 理由 |
|---|---|---|
| 目录树 | 前端 Pinia store，页面刷新重新拉取 | 目录变更不频繁 |
| 文档内容 | 前端按路径缓存，首次加载后缓存 | 用户可能反复查看同一文档 |
| 检索结果 | 不缓存，每次实时查询 | 关键词变化频繁 |
